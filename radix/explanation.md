radix sort works like this:
[explanation to be added]
it's really cool though, it works in O(N) time plus a constant depending on bit size! so if you're sorting 10000 32-bit ints, N log N algs (quick sort etc) might be better, but for higher ints, like a million, radix sort is MUCH better.

the best one in general is 8-bit radix sort (1 bit radix was my first implementation.)
